spring:
  main:
    allow-bean-definition-overriding: true
threads:
  auth:
    app:
      id: id
      secret: secret
    user-id: user-id
    redirect-uri: http://localhost:8888$/app-name/v1/threads/login/redirect
  endpoint:
    login: /app-name/v1/threads/login
    redirect: /app-name/v1/threads/login/redirect
    refresh-token: /app-name/v1/threads/refresh-token
  scheduler:
    refresh-token:
      cron: 0 0 0 * * SUN
      zone: Europe/Moscow
  polling:
    max-repeat: 20
    delay: 10s
